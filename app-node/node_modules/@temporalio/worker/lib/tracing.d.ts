import * as otel from '@opentelemetry/api';
export declare const tracer: otel.Tracer;
/**
 * Conveience function for creating a child span from an existing span
 */
export declare function childSpan(parent: otel.Span, name: string, options?: otel.SpanOptions): otel.Span;
/**
 * Wraps `fn` in a span which ends when function returns or throws
 */
export declare function instrument<T>(parent: otel.Span, name: string, fn: (span: otel.Span) => Promise<T>): Promise<T>;
