import { AbortController } from 'abort-controller';
import { ActivityFunction } from '@temporalio/workflow';
import { DataConverter } from '@temporalio/workflow/commonjs/converter/data-converter';
import { coresdk } from '@temporalio/proto';
import { Context, Info } from '@temporalio/activity';
export declare class Activity {
    readonly info: Info;
    protected readonly fn: ActivityFunction<any[], any>;
    protected readonly args: any[];
    readonly dataConverter: DataConverter;
    readonly heartbeatCallback: Context['heartbeat'];
    protected cancelRequested: boolean;
    readonly context: Context;
    cancel: (reason?: any) => void;
    readonly abortController: AbortController;
    constructor(info: Info, fn: ActivityFunction<any[], any>, args: any[], dataConverter: DataConverter, heartbeatCallback: Context['heartbeat']);
    run(): Promise<coresdk.activity_result.IActivityResult>;
}
