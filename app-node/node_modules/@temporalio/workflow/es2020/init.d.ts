import { ActivityOptions, Workflow } from './interfaces';
import { Runtime } from './internals';
export declare function overrideGlobals(randomnessSeed: number[]): void;
export declare function initWorkflow(workflowId: string, randomnessSeed: number[], taskQueue: string, activityDefaults: ActivityOptions, runtime: Runtime): void;
export declare function registerWorkflow(workflow: Workflow): void;
