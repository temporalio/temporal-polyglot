declare class AsyncLocalStorage<T> {
    asyncIdToResource: Map<number, T>;
    resourceToAsyncIds: Map<T, Set<number>>;
    unregisterQueue: Set<string>;
    protected _register(resource: T, asyncId?: number): void;
    protected _unregister(resource: T): void;
    _link(id: number, trigger: number): void;
    getStore(): T | undefined;
    run<A extends any[], R>(store: T, callback: (...args: A) => Promise<R>, ...args: A): Promise<R>;
}
export { AsyncLocalStorage };
